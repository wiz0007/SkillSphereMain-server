import express from "express";
import { addReview, approveRecordedCourseAccess, approveTuitionEnrollment, cancelTuitionEnrollment, createCourse, deleteCourse, getAllCourses, getCourseById, getMyCourses, getMyTuitionEnrollments, pauseTuitionEnrollment, getSavedCourses, rateCourse, rejectRecordedCourseAccess, rejectTuitionEnrollment, requestRecordedCourseAccess, requestTuitionEnrollment, resumeTuitionEnrollment, saveCourse, toggleCoursePublishStatus, unsaveCourse, updateCourse, } from "../controllers/course.controller.js";
import { optionalAuth } from "../middlewares/optionalAuth.js";
import { protect } from "../middlewares/protect.js";
import { loginLimiter } from "../middlewares/rateLimiter.js";
import { validate } from "../middlewares/validate.js";
import { createCourseSchema, ratingSchema, reviewSchema, } from "../validators/course.validator.js";
const router = express.Router();
router.get("/", getAllCourses);
router.post("/", protect, validate(createCourseSchema), createCourse);
router.get("/my", protect, getMyCourses);
router.get("/tuition-enrollments/mine", protect, getMyTuitionEnrollments);
router.get("/saved", protect, getSavedCourses);
router.get("/:id", optionalAuth, getCourseById);
router.post("/:id/recorded-access", protect, requestRecordedCourseAccess);
router.post("/:id/tuition-enrollments", protect, requestTuitionEnrollment);
router.put("/:id", protect, validate(createCourseSchema), updateCourse);
router.patch("/:id/publish", protect, toggleCoursePublishStatus);
router.delete("/:id", protect, deleteCourse);
router.patch("/recorded-access/:accessId/approve", protect, approveRecordedCourseAccess);
router.patch("/recorded-access/:accessId/reject", protect, rejectRecordedCourseAccess);
router.patch("/tuition-enrollments/:enrollmentId/approve", protect, approveTuitionEnrollment);
router.patch("/tuition-enrollments/:enrollmentId/reject", protect, rejectTuitionEnrollment);
router.patch("/tuition-enrollments/:enrollmentId/pause", protect, pauseTuitionEnrollment);
router.patch("/tuition-enrollments/:enrollmentId/resume", protect, resumeTuitionEnrollment);
router.patch("/tuition-enrollments/:enrollmentId/cancel", protect, cancelTuitionEnrollment);
router.post("/:id/rate", protect, loginLimiter, validate(ratingSchema), rateCourse);
router.post("/:id/review", protect, loginLimiter, validate(reviewSchema), addReview);
router.post("/:id/save", protect, saveCourse);
router.delete("/:id/save", protect, unsaveCourse);
export default router;
//# sourceMappingURL=course.routes.js.map